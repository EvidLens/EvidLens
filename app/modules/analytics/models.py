# Analytics module has no direct tables
# It aggregates data from invoicing, inventory, hr, and support
# Keeping this file empty so the module structure is consistent

from app.core.db import Base
